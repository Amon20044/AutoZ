/**
 * AutoZ Engine — root barrel export.
 *
 * Usage:
 *   import { AutoZEngine, buildPartRegistry, detectParts, computeNormalization } from '@/engine'
 *   import { AutoZEngine } from '@/engine'   // just the engine class
 */
export * from './math/index.js'
export * from './core/index.js'
